spring:
  application:
    name: administration-service-vuelo
    
server:
  port: 8082

eureka:
   client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/
   instance:
    hostname: localhost
      